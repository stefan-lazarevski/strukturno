//Да се напише програма која од стандарден влез чита еден двоцифрен и еден трицифрен број. 
//На излез да се отпечати производот на цифрите кои се наоѓаат на позицијата на десетките кај двата броја.

#include <stdio.h>

int main()
{
    int a, b, p;
    scanf("%d %d", &a, &b);
    p=(a/10)*((b/10)%10);
    printf("%d",p);


    return 0;
}
